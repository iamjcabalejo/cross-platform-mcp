# Optional. Defaults to process.cwd() (run from repo root).
# Set to the absolute path of the repo that contains .cursor/ (commands, skills, agents, rules, hooks).
# CURSOR_CONFIG_PATH=/absolute/path/to/MCP
